"""Deprecated PDF → hardcoded IEEE JATS XML pipeline.

Moved out of the live Hybrid AI path in Phase 1 cleanup.  Reference only —
use ``text_extractor`` → ``document_pipeline`` → ``scope_resolver`` →
``llm_semantic_mapper`` (Phase 2+) for production.
"""
